<h1>Editing contact</h1>

<%= render "form", contact: @contact %>

<br>

<div>
  <%= link_to "Show this contact", @contact, class:"btn btn-secondary mt-5" %>
  <%= link_to "Back to contacts", contacts_path, class:"btn btn-secondary mt-5" %>
</div>
